# Skrip dan berkas konfigurasi untuk server Linux harus tetap LF.
# Git di Windows mengubahnya ke CRLF saat checkout, dan bash di server
# menolak baris berakhiran  - itu yang menggagalkan proxmox-firewall.sh.
deploy/** text eol=lf
*.sh text eol=lf
